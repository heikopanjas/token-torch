enum GeneralSettingsCopy {
    static var providersTableHint: String {
        "Drag rows to reorder menu bar views. Use Enabled to show or hide each view; enabling fetches fresh data (subscription views may prompt for Keychain access once). Configure subscription credentials and Admin API keys on each provider tab. \(AppBrand.displayName) stores copies in its Keychain only; \(SettingsCopy.vendorCredentialPolicy)"
    }
}
