enum InfoSettingsCopy {
    static var vendorCredentialSourcesHint: String {
        "\(AppBrand.displayName) lists the vendor source recorded when each enabled subscription credential was imported. Existing copies from older builds may show that the source was not recorded. Secret token values are never displayed."
    }
}
